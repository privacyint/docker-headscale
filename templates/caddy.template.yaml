{
  storage sqlite "/data/caddy.sqlite3" {
  }
}

{$PUBLIC_SERVER_URL}:{$PUBLIC_LISTEN_PORT:443} {
  root /admin /data/admin-gui/admin
  encode gzip zstd
  try_files {path}.html {path}
  file_server

  reverse_proxy 127.0.0.1:8080

  tls {
    dns cloudflare {$CF_API_TOKEN}
  }
}
